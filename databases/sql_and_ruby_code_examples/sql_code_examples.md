```SQL
-- SELECT release-year FROM albums
--  WHERE title = 'Bossanova';

-- UPDATE albums
--  SET release_year = '2000', title = 'Whatever'
--  WHERE id = '1';

-- DELETE FROM albums WHERE id = '1';

-- INSERT INTO artists
--     (name, genre)
--     VALUES('Massive Attack', 'Alternative');

-- INSERT INTO albums
--     (title, release_year)
--     VALUES('Mezzanine', '1998');
```
